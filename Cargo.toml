[package]
name = "chumsky-proc-macro"
version = "0.1.7"
edition = "2021"
description = "Utils for parsing proc macros using chumsky"
license = "0BSD"
repository = "https://github.com/alex-s168/chumsky-proc-macro.git"

[dependencies]
chumsky = { version = "^0.12", features = [], default-features = false }
proc-macro2 = "1.0.95"

[dev-dependencies]
quote = "1.0.40"

